# Dr.R L - Production Requirements
# Install with: pip install -r requirements.txt

# Core Web Framework (stable versions)
Flask==3.0.3
flask-cors==4.0.1
gunicorn==23.0.0
Werkzeug==3.0.3

# HTTP Requests (keep-alive, retries)
requests==2.32.3
urllib3==2.2.2
certifi==2024.7.4

# HTML/XML Parsing (lightweight)
beautifulsoup4==4.12.3
lxml==5.2.2
html5lib==1.1

# Data Processing (lightweight alternatives)
# Note: pandas/numpy removed for Render free tier compatibility
# If needed, use: pip install pandas numpy (separate requirements-heavy.txt)

# Academic APIs
arxiv==2.1.3

# PDF Processing (optional - remove if not needed to save memory)
# PyMuPDF==1.24.5  # Commented out - heavy dependency
# fitz  # Alternative lightweight PDF lib if needed

# Utilities
python-dateutil==2.9.0.post0
tqdm==4.66.4
python-dotenv==1.0.1

# Security & Performance
blinker==1.8.2
charset-normalizer==3.3.2
idna==3.7

# Production monitoring (optional)
# sentry-sdk==2.5.1  # Uncomment for error tracking
